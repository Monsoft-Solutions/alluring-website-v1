import { AdsChangesView } from '@/components/ads/ads-changes.view'

export const metadata = {
    title: 'Ads changes & tracking | Admin',
    description: 'Account change history and conversion tracking health',
}

export default function AdsChangesPage() {
    return <AdsChangesView />
}
