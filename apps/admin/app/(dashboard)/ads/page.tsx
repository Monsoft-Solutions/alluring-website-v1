import { AdsOverviewView } from '@/components/ads/ads-overview.view'

export const metadata = {
    title: 'Ads | Admin',
    description: 'Google Ads spend against the leads it produced',
}

export default function AdsPage() {
    return <AdsOverviewView />
}
