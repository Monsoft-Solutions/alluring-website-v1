/**
 * Google Search Console data types for SEO insights
 *
 * These types moved to `@workspace/seo/search-console` when the data layer was
 * shared with the MCP server. This module re-exports them so existing admin
 * imports keep working; new code can import from the package directly.
 *
 * @module @/lib/types/search-console/search-console
 */
export type * from '@workspace/seo/search-console'
